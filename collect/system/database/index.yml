service:
  - key: database_download
    module: empty
    http: true
    params:
      path:
        template: '{{get_key `dataSourceName`}}'
      response_name:
        default: "price.db"
    handler_params:
      - key: file2result
        path: "[path]"
        result_name: "[response_name]"